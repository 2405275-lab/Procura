{
  "name": "procura-root",
  "version": "1.0.0",
  "description": "Procura Procurement Platform Root Command Delegate",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "install-frontend": "npm install --prefix frontend"
  }
}
